#include <stdlib.h>
#include <stdio.h>

int main(int argc, char const *argv[]) {
  printf("Hello World Bonjour. \n");
  return 0;

  /* while(*argv){
    fprintf(stdout, "Arg : %s\n", * argv ++ );
  }
  exit(EXIT_SUCCESS); */
}
